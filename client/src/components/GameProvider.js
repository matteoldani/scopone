import React from "react";

const GameContext = React.createContext();
const INITIAL_STATE = { table: "", username: "" };

const GameProvider = () => {
  return <div></div>;
};

export default GameProvider;
